{{!-- Homepage / Image Left Style --}}

{{#page}}
  <div class='o-grid'>
    <div class='o-grid__col o-grid__col--center o-grid__col--9-10-m o-grid__col--2-3-l {{#if feature_image}}o-grid__col--4-4-l{{/if}}'>
      <div class='c-home {{#unless feature_image}}c-home--content-only{{/unless}}'>
        {{#if feature_image}}
          <div class='c-home__media'>
            <img class='c-home__image lazyload' src='{{ img_url feature_image size='home-image-default' format='webp' }}' alt='{{ @site.title }}'>
          </div>
        {{/if}}

        <div class='c-home__content {{#unless feature_image}}c-home__content--only{{/unless}}'>
          <section class='c-content'>
            {{ content }}

            {{> home/subscribe-form }}
          </section>
        </div>
      </div>
    </div>
  </div>
{{/page}}