{{!--

  # Subscribe form box #

  ## Visible on:
  1. Homepage bottom after the recent posts section
  2. Post page after the author box

  ## This partial is called on the following theme files:
  1. home.hbs
  2. post.hbs

  ## Visible if:
  1. The user is not a member
  1. The website *Subscription access* is `Anyone can sign up`

  ## Hidden if:
  1. The user is already a member
  2. The website *Subscription access* is set to `Nobody` or `Only people I invite`
--}}

{{#unless @member}}
  {{#if @site.members_enabled}}
    {{#unless @site.members_invite_only}}
        <div class='{{#is 'home'}} o-grid {{/is}} is-membership'>
          <div class='{{#is 'home'}} o-grid__col o-grid__col--center o-grid__col--9-10-m o-grid__col--2-3-l {{/is}}'>
            <section class='c-subscribe'>
              <div class='c-subscribe__content'>
                <h2 class='c-subscribe__heading u-type-500 u-mb-8'>{{t '{siteTitle} Newsletter' siteTitle=@site.title}}</h2>
                <p class='u-mb-16'>{{t 'Join the newsletter to receive the latest updates in your inbox.' }}</p>
                {{>
                  subscribe-form
                  nameLabel='subscribe-box-name'
                  emailLabel='subscribe-box-email'
                }}
              </div>
              <div class='c-subscribe__icon'>
                {{> subscribe-box-icon }}
              </div>
            </section>
          </div>
        </div>
    {{/unless}}
  {{/if}}
{{/unless}}